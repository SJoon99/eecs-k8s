---
# API schema mirrored from the ObjectBucketClaim CRD installed by Rook v1.19.0.
# This CRD is installed only in the Karmada API; member clusters receive their
# full Rook/OBC APIs through the Infra path.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: objectbucketclaims.objectbucket.io
spec:
  conversion:
    strategy: None
  group: objectbucket.io
  names:
    kind: ObjectBucketClaim
    listKind: ObjectBucketClaimList
    plural: objectbucketclaims
    shortNames:
      - obc
      - obcs
    singular: objectbucketclaim
  scope: Namespaced
  versions:
    - name: v1alpha1
      schema:
        openAPIV3Schema:
          properties:
            spec:
              properties:
                additionalConfig:
                  nullable: true
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                bucketName:
                  type: string
                generateBucketName:
                  type: string
                objectBucketName:
                  type: string
                storageClassName:
                  type: string
              type: object
            status:
              type: object
              x-kubernetes-preserve-unknown-fields: true
          type: object
      served: true
      storage: true
      subresources:
        status: {}
